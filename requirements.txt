# SmartSafe AI PPE Detection - Render.com Optimized
# Core Computer Vision
opencv-python==4.8.1.78
numpy==1.24.3
Pillow>=9.0.0

# Deep Learning Framework (Production Ready - Auto GPU/CPU)
# PyTorch with CUDA support for production
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
torchaudio>=2.0.0  # Audio processing support

# Web Framework
flask>=2.3.0
flask-cors>=4.0.0
flask-limiter>=3.5.0
Flask-Mail==0.9.1  # Specific version for stability
waitress>=2.1.2
gunicorn>=21.2.0
Werkzeug>=2.3.7  # Required by Flask

# Database & Networking
SQLAlchemy>=2.0.0
psycopg2-binary==2.9.7
redis>=5.0.1
dnspython>=2.4.0  # DNS resolution for camera detection
certifi>=2023.7.22  # SSL certificate verification

# Security & Authentication
PyJWT>=2.8.0
cryptography>=41.0.7
bcrypt==4.0.1
pyotp>=2.8.0

# Email & Validation
email-validator>=2.0.0

# Data Processing & Visualization
pandas>=1.5.0
scipy>=1.9.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Configuration & Environment
pyyaml>=6.0
python-dotenv==1.0.0

# HTTP & Networking
requests==2.31.0
urllib3>=2.0.0
charset-normalizer>=3.0.0

# Monitoring & Health
psutil>=5.9.0
schedule>=1.2.0
GPUtil>=1.4.0
prometheus-client>=0.19.0
APScheduler>=3.10.4

# Development & Progress
tqdm>=4.64.0

# Export & QR Code
openpyxl>=3.1.0
qrcode>=7.4.0

# Logging
loguru>=0.7.2

# Audio Support
pygame>=2.5.2

# Additional Python Standard Library Extensions
pathlib2>=2.3.0

# Production Deployment Support
python-multipart>=0.0.6  # File upload support

# Note: The following packages are built-in to Python 3.7+ and don't need to be installed:
# - concurrent.futures (built-in since Python 3.2)
# - dataclasses (built-in since Python 3.7)
# - ipaddress (built-in since Python 3.3)
# - uuid (built-in)
# - traceback (built-in)
# - gc (built-in)
# - socket (built-in)
# - base64 (built-in)
# - queue (built-in)
# - collections (built-in)
# - deque (built-in)
# - io (built-in)
# - BytesIO (built-in)
# - threading (built-in)
# - time (built-in)
# - datetime (built-in)
# - timedelta (built-in)
# - json (built-in)
# - os (built-in)
# - logging (built-in)
# - typing (built-in)
# - pathlib (built-in)
# - zipfile (built-in)
# - subprocess (built-in)
# - platform (built-in)
# - tempfile (built-in)
# - random (built-in)
# - ssl (built-in)
# - dns (provided by dnspython) 


